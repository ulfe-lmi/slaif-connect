#!/bin/sh
set -eu

# Shell helper placeholder for site launcher integrations. The reference
# implementation maps payloadId to repository-owned templates in
# lib/launcher-intent.py.
slaif_slurm_profile_contract() {
  printf '%s\n' 'Slurm profiles are site-approved resource metadata, not arbitrary shell commands'
}
